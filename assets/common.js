(function(){
  // --- inject header bar ---
  const bar = document.createElement('div');
  bar.id = 'ai-gallery-bar';
  bar.innerHTML = `
    <a href="/AIGameGallery/">← Back to Gallery</a>
    <button id="ai-about-toggle">About this version</button>
  `;
  document.body.append(bar);

  // --- inject hidden about box ---
  const about = document.createElement('div');
  about.id = 'ai-gallery-about';
  // You can replace this text per-page if you want;
  // or leave a generic placeholder.
  about.innerHTML = window.AI_GAME_NOTES || `
      <strong>About this game</strong><br>
      This version was generated by an AI model. Use arrow keys / WASD to play.
  `;
  document.body.append(about);

  // --- toggle logic ---
  document.getElementById('ai-about-toggle')
          .addEventListener('click',()=>{
            about.style.display =
              about.style.display==='block' ? 'none' : 'block';
          });

  // --- offset canvas so the bar doesn't hide the top edge (optional) ---
  document.body.style.paddingTop = '3.2rem';
})();
